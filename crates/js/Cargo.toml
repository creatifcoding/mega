[package]
name = "js"
version = "0.1.0"
edition = "2021"
build = "../codegen/etc/build.rs"

[lib]
path = "src/lib.rs"

[dependencies]
emacs-sys.path = "../emacs-sys"
lisp-macros.path = "../lisp-macros"
lisp-util.path = "../lisp-util"
lsp-json.path = "../lsp-json"
libc.workspace = true
futures = "0.3"
# Deno dependencies - using compatible crates.io versions
# deno_runtime 0.229 requires deno_core 0.363
deno_core = "0.363"
deno_runtime = "0.229"
serde_json = { version = "1.0", features = ["preserve_order"] }
tokio = { workspace = true, features = ["full"] }
url = "2.5"
anyhow = "1.0"

[build-dependencies]
codegen = { path = "../codegen" }